{
  "scripts": {
    "dev": "tailwindcss -i styles.css -o public/styles.css --watch",
    "build": "tailwindcss build styles.css -o public/styles.css --minify",
    "deploy": "sh deploy.sh > deploy.log 2>&1"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.9",
    "tailwindcss": "^3.3.3"
  }
}
